<% provide(:title, "Home") %>

<% if loged_in? %>
  <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <%= render 'shared/home_post' %>
      </section>
    </aside>
    <div class="col-md-8">
      <h3> Micropost Feed</h3>
      <%= render 'shared/feed'%>
    </div>
  </div>
<% else %>
      <%= render 'shared/home_row' %>
<% end %>
</div>
